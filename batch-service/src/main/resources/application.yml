spring:
  application:
    name: batch-service
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: embedded

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info
